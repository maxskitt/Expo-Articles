import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
  container: {
    position: 'fixed',
    top: '0',
    left: '0',
  },
  logoText: {
    fontSize: 20,
    color: '#EEAB02',
  },
});

export default styles;
